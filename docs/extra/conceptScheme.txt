main.py
│
├── chiama → hardware/board.py (inizializza pin, bus, sicurezza)
│
├── crea → state_machine.py
│         │
│         ├── importa → states/__init__.py
│         │           ├── base_state.py (classe astratta)
│         │           ├── line_follow.py
│         │           ├── intersection.py
│         │           ├── obstacle.py
│         │           ├── rescue_zone.py
│         │           ├── exit_zone.py
│         │           └── end.py
│         │
│         └── loop principale → chiama execute() dello stato corrente
│
├── importa → sensors/__init__.py
│         ├── line_sensor.py (lettura linea)
│         ├── tof_sensor.py (distanza ostacoli)
│         ├── color_sensor.py (zona bianca / rescue)
│         └── imu.py (orientamento)
│
├── importa → controllers/__init__.py
│         ├── pid.py (calcola correzioni)
│         └── motor_controller.py (aziona motori)
│
├── importa → actuators/__init__.py
│         └── rescue_arm.py (servo/braccio per oggetti)
│
├── importa → utils/
│         ├── timer.py (ritardi precisi, debounce)
│         ├── logger.py (debug/log)
│         └── constants.py (valori fissi e limiti)
│
└── importa → config.py (parametri modificabili senza toccare logica)
